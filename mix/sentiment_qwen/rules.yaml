# Правила для RTS sentiment_qwen backtest.
#
# Правила матчатся сверху вниз, по первому совпадению.
# action: follow — торгуем по знаку sentiment (LONG если >0, SHORT если <0)
# action: invert — инвертируем (SHORT если >0, LONG если <0)
# action: skip   — не торгуем
#
# Диапазон, не покрытый ни одним правилом, — пропускается (эквивалент skip).
# Можно свободно добавлять, удалять и переставлять строки.
#
# Ниже — стартовая конфигурация для qwen2.5:14b.

# rules:
#   - {min: -10, max: -6, action: follow}
#   - {min: -5,  max: -3, action: invert}
#   - {min: -2,  max:  2, action: follow}
#   - {min:  3,  max:  5, action: invert}
#   - {min:  6,  max:  7, action: follow}
#   - {min:  8,  max: 10, action: invert}

# rules:  # qwen2.5:14b
#   - {min: -10, max: -3, action: follow}
#   - {min:  -2, max: 10, action: invert}

rules:  # qwen3:14b
  - {min: -10, max: -6, action: invert}
  - {min: -5,  max: -3, action: follow}
  - {min: -2,  max: -1, action: invert}
  - {min:  0,  max:  2, action: follow}
  - {min:  3,  max:  3, action: invert}
  - {min:  4,  max:  4, action: follow}
  - {min:  5,  max:  6, action: invert}
  - {min:  7,  max:  7, action: follow}
  - {min:  8,  max: 10, action: invert}
  